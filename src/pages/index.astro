---
import Layout from '../components/layout.astro';

// Your project data
const projects = [
  {
    title: "E-Commerce App Design",
    description: "Designed a modern e-commerce application with focus on user experience and intuitive navigation. Conducted user research and created wireframes and prototypes.",
    type: "app",
    icon: "🛍️"
  },
  {
    title: "Portfolio Website UI", 
    description: "Created a responsive portfolio website design with modern aesthetics and smooth interactions. Focused on accessibility and mobile-first design.",
    type: "website",
    icon: "🌐"
  },
  {
    title: "Mobile Game Interface",
    description: "Designed user interface for a mobile puzzle game with engaging interactions and smooth animations. Created design system and component library.",
    type: "game", 
    icon: "🎮"
  },
  {
    title: "Dashboard Redesign",
    description: "Redesigned analytics dashboard for better data visualization and user workflow. Improved information architecture and user flow.",
    type: "web app",
    icon: "📊"
  }
];
---

<style>
  .hero-section {
    margin: 2rem 0 4rem 0;
    width: 100vw;
    position: relative;
    left: 50%;
    right: 50%;
    margin-left: -50vw;
    margin-right: -50vw;
  }

  .hero-container {
    position: relative;
    width: 100%;
    max-width: none;
    margin: 0 auto;
    padding: 0 1rem; /* 1rem padding for the image only */
  }

  .hero-image {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 0;
  }

  .hero-content {
    position: absolute;
    bottom: 3rem;
    right: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 1.5rem;
    z-index: 10;
    width: 100%;
    max-width: 1200px; /* Same as main content */
    margin: 0 auto;
    padding: 0 2rem; /* 2rem padding for text/buttons to align with content */
    box-sizing: border-box;
    left: 50%; /* Center the content container */
    transform: translateX(-50%); /* Center the content container */
  }

  .hero-text {
    color: rgba(255, 255, 255, 0.884);
    font-weight: 700;
    line-height: 1.1;
    text-align: right;
    padding: 0rem 0.5rem;
    
    /* Responsive font scaling that works for 3 lines */
    font-size: clamp(1.25rem, 3vw + 0.5rem, 2.5rem);
    
    /* Ensure text stays within bounds */
    max-width: 600px;
    margin-left: auto;
  }

  /* Rest of your button styles remain exactly the same */
  .hero-buttons {
    display: flex;
    gap: 1rem;
    align-items: center;
  }

  .hero-btn {
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
  }

  .hero-btn.circle {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    isolation: isolate; /* Creates new stacking context */
      
      /* TRUE Glass Morphism - transparent background */
      background: rgba(255, 255, 255, 0.05);
      
      /* Enhanced glass effect */
      backdrop-filter: 
        blur(10px) 
        saturate(180%) 
        contrast(120%) 
        brightness(110%);
      -webkit-backdrop-filter: 
        blur(10px) 
        saturate(180%) 
        contrast(120%) 
        brightness(110%);
      
      /* Glass border */
      border: 1px solid rgba(255, 255, 255, 0.15);
      
      /* Subtle shadow for depth */
      box-shadow: 
        0 8px 32px 0 rgba(0, 0, 0, 0.36),
        inset 0 1px 0 0 rgba(255, 255, 255, 0.1);
  }

  .hero-btn.circle::before {
    content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.1) 0%,
        rgba(255, 255, 255, 0.05) 40%,
        transparent 60%,
        rgba(255, 255, 255, 0.02) 100%
      );
      border-radius: 24px;
      pointer-events: none;
      z-index: 1;
  }

  .hero-btn.circle::after {
    content: '';
      position: absolute;
      top: 0%;
      left: 0%;
      right: 0%;
      bottom: 0%;
      border-radius: 50%;
      background-image: 
        radial-gradient(
          ellipse at 30% 20%,
          rgba(194, 194, 194, 0.3) 0%,
          transparent 50%
        ),
        radial-gradient(
          ellipse at 70% 80%,
          rgba(255, 255, 255, 0.2) 0%,
          transparent 50%
        );
      opacity: 0.4;
      animation: glassShift 15s ease-in-out infinite;
      pointer-events: none;
      z-index: -1;
      mix-blend-mode: overlay;
  }

  .hero-btn.rectangle {
    height: 60px;
    padding: 0 2rem;
    border-radius: 30px;
    color: rgba(255, 255, 255, 0.603);
    border: 2px solid rgba(255, 255, 255, 0.3);
    font-family: var(--font-primary);
    font-weight: 700;
    font-size: 1.1rem;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    isolation: isolate; /* Creates new stacking context */
      
      /* TRUE Glass Morphism - transparent background */
      background: rgba(255, 255, 255, 0.05);
      
      /* Enhanced glass effect */
      backdrop-filter: 
        blur(10px) 
        saturate(180%) 
        contrast(120%) 
        brightness(110%);
      -webkit-backdrop-filter: 
        blur(10px) 
        saturate(180%) 
        contrast(120%) 
        brightness(110%);
      
      /* Glass border */
      border: 1px solid rgba(255, 255, 255, 0.15);
      
      /* Subtle shadow for depth */
      box-shadow: 
        0 8px 32px 0 rgba(0, 0, 0, 0.36),
        inset 0 1px 0 0 rgba(255, 255, 255, 0.1);
  }

  .hero-btn.rectangle::before {
    content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.1) 0%,
        rgba(255, 255, 255, 0.05) 40%,
        transparent 60%,
        rgba(255, 255, 255, 0.02) 100%
      );
      border-radius: 24px;
      pointer-events: none;
      z-index: 1;
  }

  .hero-btn.rectangle::after {
    content: '';
      position: absolute;
      top: 0%;
      left: 0%;
      right: 0%;
      bottom: 0%;
      border-radius: 30%;
      background-image: 
        radial-gradient(
          ellipse at 30% 20%,
          rgba(184, 184, 184, 0.3) 0%,
          transparent 50%
        ),
        radial-gradient(
          ellipse at 70% 80%,
          rgba(252, 252, 252, 0.2) 0%,
          transparent 50%
        );
      opacity: 0.4;
      animation: glassShift 15s ease-in-out infinite;
      pointer-events: none;
      z-index: -1;
      mix-blend-mode: overlay;
  }

  .hero-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4);
  }

  .hero-btn.circle:hover {
    background: rgba(70, 70, 70, 0.8);
    border-color: rgba(255, 255, 255, 0.5);
  }

  .hero-btn.rectangle:hover {
    background: rgba(70, 70, 70, 0.8);
    border-color: rgba(255, 255, 255, 0.5);
    color: rgba(255, 255, 255, 0.76);
  }

  .btn-icon {
    width: 24px;
    height: 24px;
    opacity: 60%;
  }

  .hero-btn.circle:hover .btn-icon {
    opacity: 100%;
  }
  

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .hero-container {
      padding: 0 1rem; /* Keep 1rem padding for image */
    }
    
    .hero-content {
      bottom: 1.5rem;
      gap: 1rem;
      padding: 0 1rem; /* Match image padding on mobile if needed */
    }
    
    .hero-text {
      font-size: clamp(1.1rem, 4vw, 1.5rem);
      line-height: 1.2;
    }
    
    .hero-btn.circle {
      width: 50px;
      height: 50px;
    }
    
    .hero-btn.rectangle {
      height: 50px;
      padding: 0 1.5rem;
      font-size: 0.9rem;
    }
    
    .btn-icon {
      width: 20px;
      height: 20px;
    }
  }

  @media (max-width: 480px) {
    .hero-content {
      bottom: 1rem;
    }
    
    .hero-text {
      font-size: 1rem;
      max-width: 300px;
    }
  }

  .projects-section {
    margin: 4rem 0;
  }

  .section-label {
    font-size: 2.5rem;
    font-weight: 800;
    opacity: 85%;
    margin-bottom: 1rem;
    text-align: left;
  }

  .divider {
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
    margin: 2rem 0 3rem 0;
  }

  /* Temporary hero placeholder */
  .hero-placeholder {
    background: linear-gradient(135deg, rgba(255,255,255,0.1) 0%, rgba(255,255,255,0.05) 100%);
    border-radius: 20px;
    padding: 4rem 2rem;
    border: 1px solid rgba(255,255,255,0.1);
    backdrop-filter: blur(10px);
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 600;
    margin-bottom: 2rem;
    margin-left: 2rem;
    background: linear-gradient(45deg, #fff, #a0a0a0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 2rem;
  }


</style>

<Layout
  title="Apoorv Singh - UX Design Intern"
  description="Creating intuitive digital experiences through user-centered design"
>
  <!-- Hero Section -->
  <section class="hero-section">
    <div class="hero-container">
      <img src="/images/Grid Updated.png" alt="Apoorv Singh - UX Design Intern" class="hero-image" />
      
      <!-- Text and buttons container -->
      <div class="hero-content">
        <div class="hero-text">
          I am a UX Designer focused <br />
          on creatingaccessible and<br />
          engaging user experiences
        </div>
        <div class="hero-buttons">
          <a href="https://www.linkedin.com/in/apoorv-singh-87377021b/" class="hero-btn circle" target="_blank">
            <img src="/images/linked in logo.png" alt="LinkedIn" class="btn-icon" />
          </a>
          <button class="hero-btn rectangle">Toolkit</button>
        </div>
      </div>
    </div>
  </section>

<!-- Projects Section -->
<section class="projects-section">
  <h2 class="section-label">Projects</h2>
  <div class="divider"></div>
  
  <!-- Project Cards Grid -->
  <div class="projects-grid">
    {projects.map(project => (
      <article class="project-card">
        <div class="project-left">
          <div class="project-icon">{project.icon}</div>
          <span class="project-type">{project.type}</span>
        </div>
        <div class="project-right">
          <h3 class="project-title">{project.title}</h3>
          <p class="project-description">
            {project.description}
          </p>
        </div>
      </article>
    ))}
  </div>
</section>
</Layout>